PYTHON

Podemos trabajar en python y luego pasarlo al bash de ubuntu, trabjaar desde ahí. 
jupyter-notebook 

notebook: opcion escribir texto, comandos, que resultado nos ha dado y la conclusiion
ctrl + h = help
No vemos el historial ejecutado mientras que en Ipython si

Output: celda es lo utimo que hemos pedido
shit+tab obtienes información sobre el comando
escribir algo seleccionas todo y le das a las comillas o lo que quieras cerrar

lista: []
tupla: separado por coma (). No cambia lo que tiene hasta que llegues a una lista
diccionario: {} 
ininstance(): para saber que tipo de objeto es
hasattr(): es la versión correcta y si tiene determinado atributo
dir(objeto): saber que atributos tiene un objeto
hash: siempre te da la misma entrada, no puedes ir atras. Ejemplo: tarjeta american express
a=[1,2,3] #tengo un objeto, la variable a esta apuntando a este objeto
b=a #la variable b esta apuntando al mismo sitio que a
.copy =importante en algunos casos poner copy para mantener la original
key word pass: sirve como un to do.
import nombredelfichero.py

%lsmagic

Ubuntu and notebook
%lsmagic
%pwd

! ver lo que tenemos. 
mode = 'w' escrito 'r' 
si no existe el archivo tienes que ser w 
si existe el archivo puede hacer w y r

f = open("nombrearchivo", mode='r')
f.read
f.readline
f.seek (buscar)
f.tell (indica posicion del puntero)
f.tell? el ? ayuda y te dince que es lo que haces
with open:util para abrir archivos no acordarte de guardar y cerrar el archivo. abre el archivo, lo lees y cuando terminamos lo cierra
fw = solo lee
fa = añade

set: elementos unicos no se pueden repetir 

jupyter notebook --no-browser: acceso a todos los 
which jupyter 

arrange: numero x hasta numero y, con pasos/saltos
linspace: 

randn: gaussian mean 0 variance 1
rand: uniforme [0,1]

Funcion Heaviside Step / escalon:

array: 
matrix: valores propios, algebra lineal(trabajar con matrices)
dot: punto, cuando haces la operación. producto excalar en algebra linear (elm * elem)


integral: es el area baja la curva
derivada: crecimiento/pendiente





































































